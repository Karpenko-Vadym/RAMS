@model RAMS.ViewModels.InterviewScheduleViewModel

<!-- SCHEDULE INTERVIEW PARTIAL -->
@{
    AjaxOptions ajaxOptions = new AjaxOptions
    {
        UpdateTargetId = "edit-position-message-modal-body-div",
        OnBegin = "OnAjaxBegin",
        OnSuccess = "OnAjaxSuccess",
        OnFailure = "OnAjaxFailure",
        OnComplete = "OnAjaxComplete"
    };

}

@if (Model.CandidateId > 0)
{
    using (Ajax.BeginForm("ScheduleInterview", "Position", new { Area = "Agency" }, ajaxOptions, new { id = "schedule-interview-form" }))
    {
        @Html.AntiForgeryToken()

        @Html.HiddenFor(model => model.CandidateId)
    
        <div class="well well-lg bottom-margin-0">
            <div class="btn-group btn-group-justified" role="group">

                <div class="btn-group" role="group">
                    <input type="button" class="btn btn-info" onclick="LoadAction('schedule-interview-modal-body-div', '@Url.Action("ScheduleInterview", "Position", new { candidateId = Model.CandidateId, displayDate = Convert.ToDateTime(Model.DisplayDate).AddDays(-4).ToString("dd/MM/yyyy hh:mm:ss tt"), Area = "Agency" })');" value="Previous 4 Days" />
                </div>

                <div class="btn-group" role="group">
                    @if (Convert.ToDateTime(Model.DisplayDate).ToString("dd/MM/yyyy") == DateTime.Now.ToString("dd/MM/yyyy"))
                    {
                        <input type="button" class="btn btn-danger" disabled value="Today" />
                    }
                    else
                    {
                        <input type="button" class="btn btn-danger" onclick="LoadAction('schedule-interview-modal-body-div', '@Url.Action("ScheduleInterview", "Position", new { candidateId = Model.CandidateId, displayDate = DateTime.Now.ToString("dd/MM/yyyy hh:mm:ss tt"), Area = "Agency" })');" value="Today" />
                    }
                </div>

                <div class="btn-group" role="group">
                    <input type="button" class="btn btn-info" onclick="LoadAction('schedule-interview-modal-body-div', '@Url.Action("ScheduleInterview", "Position", new { candidateId = Model.CandidateId, displayDate = Convert.ToDateTime(Model.DisplayDate).AddDays(4).ToString("dd/MM/yyyy hh:mm:ss tt"), Area = "Agency" })');" value="Next 4 Days" />
                </div>

            </div>

            <div class="vertical-padding-24"></div>
            <div class="well well-lg bottom-margin-0 background-white">
                <div class="row">
                    <div class="col-md-6">
                        <h4 class="text-center">@Convert.ToDateTime(Model.DisplayDate).DayOfWeek @Convert.ToDateTime(Model.DisplayDate).ToString("dd/MM/yyyy")</h4>
                        <table class="table table-striped table-bordered table-hover bottom-margin-0" cellspacing="0">
                            <thead>
                                <tr class="active">
                                    <th>Time</th>
                                    <th>Availability</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr class="cursor-pointer">
                                    <td>9:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(9)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(9)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(9)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>10:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(10)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(10)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(10)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>11:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(11)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(11)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(11)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>12:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(12)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(12)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(12)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>13:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(13)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(13)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(13)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>14:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(14)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(14)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(14)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>15:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(15)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(15)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(15)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>16:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(16)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(16)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(16)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>17:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(17)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddHours(17)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddHours(17)"></span></td>
                                    }
                                </tr>
                            </tbody>
                        </table>
                    </div>

                    <div class="col-md-6">
                        <h4 class="text-center">@Convert.ToDateTime(Model.DisplayDate).AddDays(1).DayOfWeek @Convert.ToDateTime(Model.DisplayDate).AddDays(1).ToString("dd/MM/yyyy")</h4>
                        <table class="table table-striped table-bordered table-hover bottom-margin-0" cellspacing="0">
                            <thead>
                                <tr class="active">
                                    <th>Time</th>
                                    <th>Availability</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr class="cursor-pointer">
                                    <td>9:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(9)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(9)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(9)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>10:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(10)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(10)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(10)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>11:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(11)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(11)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(11)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>12:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(12)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(12)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(12)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>13:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(13)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(13)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(13)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>14:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(14)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(14)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(14)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>15:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(15)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(15)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(15)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>16:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(16)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(16)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(16)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>17:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(17)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(17)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(1).AddHours(17)"></span></td>
                                    }
                                </tr>
                            </tbody>
                        </table>
                    </div>

                    <div class="col-md-6">
                        <h4 class="text-center">@Convert.ToDateTime(Model.DisplayDate).AddDays(2).DayOfWeek @Convert.ToDateTime(Model.DisplayDate).AddDays(2).ToString("dd/MM/yyyy")</h4>
                        <table class="table table-striped table-bordered table-hover bottom-margin-0" cellspacing="0">
                            <thead>
                                <tr class="active">
                                    <th>Time</th>
                                    <th>Availability</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr class="cursor-pointer">
                                    <td>9:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(9)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(9)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(9)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>10:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(10)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(10)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(10)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>11:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(11)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(11)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(11)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>12:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(12)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(12)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(12)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>13:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(13)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(13)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(13)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>14:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(14)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(14)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(14)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>15:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(15)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(15)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(15)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>16:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(16)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(16)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(16)"></span></td>
                                    }
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>17:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(17)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(17)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(2).AddHours(17)"></span></td>
                                    }
                                </tr>
                            </tbody>
                        </table>
                    </div>

                    <div class="col-md-6">
                        <h4 class="text-center">@Convert.ToDateTime(Model.DisplayDate).AddDays(3).DayOfWeek @Convert.ToDateTime(Model.DisplayDate).AddDays(3).ToString("dd/MM/yyyy")</h4>
                        <table class="table table-striped table-bordered table-hover bottom-margin-0" cellspacing="0">
                            <thead>
                                <tr class="active">
                                    <th>Time</th>
                                    <th>Availability</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr class="cursor-pointer">
                                    <td>9:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(9)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(9)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(9)"></span></td>
                                    }  
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>10:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(10)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(10)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(10)"></span></td>
                                    }  
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>11:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(11)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(11)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(11)"></span></td>
                                    }  
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>12:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(12)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(12)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(12)"></span></td>
                                    }  
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>13:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(13)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(13)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(13)"></span></td>
                                    }  
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>14:00</td>  
                                    @if(Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(14)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(14)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(14)"></span></td>
                                    }     
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>15:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(15)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(15)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(15)"></span></td>
                                    }  
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>16:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(16)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(16)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(16)"></span></td>
                                    }  
                                </tr>
                                <tr class="cursor-pointer">
                                    <td>17:00</td>
                                    @if (Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(17)) != null)
                                    {
                                        <td>@Model.Interviews.FirstOrDefault(i => i.InterviewDate == Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(17)).Candidate.FullName</td>
                                    }
                                    else
                                    {
                                        <td>Available <span class="pull-right margin-left-10"><input type="radio" name="SelectedDateTime" value="@Convert.ToDateTime(Model.DisplayDate).AddDays(3).AddHours(17)"></span></td>
                                    }  
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <div class="vertical-padding-24"></div>

        <div class="form-group bottom-margin-0">
            <div class="col-md-offset-0 col-lg-offset-0 col-md-3 col-lg-3">
                <input type="submit" value="Schedule" class="btn btn-danger btn-block" onclick="$('#schedule-interview-modal').modal('hide');" />
            </div>

            <div class="col-md-offset-6 col-lg-offset-6 col-md-3 col-lg-3">
                <input type="button" value="Cancel" class="btn btn-info btn-block" onclick="$('#schedule-interview-modal').modal('hide');" />
            </div>
        </div>

        <div class="clearfix"></div>
    }
}
else
{
<div class="well well-lg bottom-margin-0">
    <div class="well well-lg bottom-margin-0 background-white">
        <div class="alert alert-danger bottom-margin-0" role="alert">
            <span>Oh no!<br />There is no data available.</span>
        </div>
    </div>
</div>
}
<!-- END OF SCHEDULE INTERVIEW PARTIAL -->
